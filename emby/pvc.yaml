apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: emby-config
spec:
  storageClassName: weyma-shared
  resources:
    requests:
      storage: 10Gi
  volumeMode: Filesystem
  accessModes:
    - ReadWriteOnce